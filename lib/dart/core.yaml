include: package:lints/recommended.yaml

linter:
  rules:
    - prefer_single_quotes
    - unawaited_futures
    - require_trailing_commas
    - always_use_package_imports

analyzer:
  exclude: ["**/*.g.dart", "**/*.mocks.dart", "**/*.freezed.dart"]
